const Validator = required('./Validator')
describe('Validator,'() => {
  it('has a field and configuration property')
  const nameValidator = new Validator('name', {
    type: String.
    required: true
  });
  expect(nameValidator.field.toEqual('name');
  expect(name.Validator.configuration);
  
  ))
})